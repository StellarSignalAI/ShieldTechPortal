# ShieldTech Supabase backend

- `migrations/` — SQL migrations (apply with `supabase db push` or the SQL editor)
- `functions/` — Edge Functions (deploy with `supabase functions deploy <name>`)

Secrets each function expects are documented in the root OUTSTANDING-APIS.md.
